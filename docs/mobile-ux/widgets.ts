type WidgetPlatform = "ios" | "android";
type WidgetState = keyof typeof widgetScenarioLabels;
type WidgetAppearance = "light" | "dark" | "tinted";
type WidgetEvent = "photo" | "design" | "story";

export const widgetScenarioLabels = {
  now: "During panel · 2:18",
  leave: "Leave now · 2:25",
  upcoming: "Upcoming con · 15 days",
  empty: "No planned panels",
  noConvention: "No convention",
  finished: "Plan finished · 5:00",
} as const;

export const widgetStatusCopy = {
  empty: { title: "Your plan is empty", body: "Add panels in ConPaws.", action: "Open schedule", detail: "Open your convention's schedule to add panels to your plan." },
  noConvention: { title: "Open ConPaws", body: "Choose a convention to show here.", action: "Open ConPaws", detail: "Open ConPaws to check your saved conventions and choose a convention for this widget." },
  finished: { title: "Nothing else planned", body: "Your saved panels have ended.", action: "Review plan", detail: "All three panels in this sample plan have ended. The convention continues through September 20." },
} as const;

const widgetEvents: Record<WidgetEvent, { title: string; room: string; official: string; attending: string; note: string }> = {
  photo: {
    title: "Fursuit photography", room: "Ballroom A", official: "2:00–3:00 PM", attending: "2:00–2:25 PM",
    note: "Leave at 2:25 PM. You have a 10-minute buffer before joining Character design lab.",
  },
  design: {
    title: "Character design lab", room: "Cedar", official: "2:00–3:30 PM", attending: "2:35–3:20 PM",
    note: "You plan to join after the session starts. Check the event's late-entry policy before relying on this plan.",
  },
  story: {
    title: "The art of storytelling", room: "See event details", official: "4:00–5:00 PM", attending: "4:00–5:00 PM",
    note: "You have 40 minutes free after Character design lab ends for you.",
  },
};

const widgetMark = `<svg viewBox="0 0 24 24" aria-hidden="true"><ellipse cx="5" cy="8" rx="2.2" ry="3"/><ellipse cx="10" cy="5" rx="2.2" ry="3"/><ellipse cx="16" cy="5" rx="2.2" ry="3"/><ellipse cx="21" cy="9" rx="2.2" ry="3"/><path d="M6 16c0-3 3-6 6-6s7 4 7 7c0 5-4 2-7 2s-6 2-6-3Z"/></svg>`;

const widgetStyles = `
.widget-gallery{--w-surface:#fff;--w-text:#17202d;--w-sub:#636d7d;--w-line:#e6e8ed;--w-accent:#00759f;--w-soft:#e7f4fa;--w-green:#23774c;--w-green-bg:#edf7f0;--w-wall:#e7edf3;max-width:1168px;margin:0 auto;padding:14px 0 40px;color:#091533;font-family:-apple-system,BlinkMacSystemFont,"Helvetica Neue",sans-serif;font-synthesis:none;letter-spacing:-.2px}
.widget-gallery *{box-sizing:border-box}.widget-gallery button,.widget-gallery select{font:inherit}.widget-gallery button{cursor:pointer;border:0}.widget-gallery button:focus-visible,.widget-gallery a:focus-visible,.widget-gallery select:focus-visible,.widget-gallery input:focus-visible{outline:3px solid #0faced;outline-offset:3px}.widget-gallery h2,.widget-gallery h3,.widget-gallery p{margin:0}.widget-gallery button:active{transform:none}
.widget-gallery .w-intro{display:flex;justify-content:space-between;gap:26px;align-items:end;margin:0 0 24px}.widget-gallery .w-kicker{font-size:10px;font-weight:750;letter-spacing:1.8px;text-transform:uppercase;color:#6f8296;margin-bottom:10px}.widget-gallery h2{font-size:35px;letter-spacing:-1.3px;line-height:1.1}.widget-gallery h2 span{color:#007ba8}.widget-gallery .w-intro p{max-width:390px;font-size:13px;line-height:1.6;color:#68778b}.widget-gallery .w-controls{display:flex;flex-wrap:wrap;gap:12px;align-items:center;padding:16px 0;border-top:1px solid #dce2ea;border-bottom:1px solid #dce2ea;margin-bottom:22px}.widget-gallery .w-segment{display:inline-flex;background:#e6eaf0;padding:4px;border-radius:12px;gap:2px}.widget-gallery .w-segment button{border-radius:9px;min-height:36px;padding:7px 15px;color:#5d6e82;font-size:12px;font-weight:650;background:transparent}.widget-gallery .w-segment button[aria-pressed=true]{background:#fff;color:#091533;box-shadow:0 1px 4px #192b4217}.widget-gallery .w-control-label{display:flex;align-items:center;gap:7px;font-size:12px;color:#5b6b80}.widget-gallery .w-control-label select{background:#fff;border:1px solid #d6dde6;border-radius:8px;color:#24334b;min-height:36px;padding:6px 8px}.widget-gallery .w-check{margin-left:auto;min-height:36px}.widget-gallery .w-check input{width:17px;height:17px;accent-color:#007ba8}.widget-gallery .w-sample-line{display:flex;justify-content:space-between;gap:12px;font-size:11px;color:#68778b;margin-bottom:15px;line-height:1.5}.widget-gallery .w-sample-line strong{color:#3d536d;font-weight:650}.widget-gallery .w-grid{display:grid;grid-template-columns:190px minmax(300px,1fr) minmax(300px,1fr);gap:24px;align-items:start}.widget-gallery .w-caption{display:flex;align-items:baseline;justify-content:space-between;gap:5px;font-size:12px;font-weight:650;margin:0 0 12px;color:#334963}.widget-gallery .w-caption small{font-size:10px;font-weight:500;color:#7a899c}.widget-gallery .w-canvas{background:var(--w-wall);border:1px solid #dbe2e9;border-radius:24px;display:flex;align-items:center;justify-content:center;padding:19px 12px;min-height:215px}.widget-gallery .w-canvas-large{min-height:403px}.widget-gallery .w-explain{font-size:12px;line-height:1.6;color:#68778b;margin-top:13px}.widget-gallery .w-explain strong{display:block;color:#354d66;font-size:12px;margin-bottom:3px}.widget-gallery .w-design-note{padding:18px 0 0;margin-top:20px;border-top:1px solid #dfe5ec;display:flex;gap:13px;align-items:start}.widget-gallery .w-note-number{font-size:10px;font-weight:700;color:#00779c;background:#e3f1f7;padding:5px 7px;border-radius:6px}.widget-gallery .w-design-note p{font-size:12px;color:#68778b;line-height:1.65}.widget-gallery .w-design-note strong{color:#334b65}.widget-gallery .w-rendering{font-size:10px;line-height:1.5;color:#718197;margin:16px 0 0}.widget-gallery .w-surface{color:var(--w-text);background:var(--w-surface);border-radius:23px;padding:15px;box-shadow:0 3px 8px #172a4513;position:relative;text-align:left;display:flex;flex-direction:column;min-width:0;overflow:hidden;letter-spacing:-.25px}.widget-gallery button.w-surface:hover,.widget-gallery .w-tappable:hover{filter:brightness(.985)}
.widget-gallery .w-small{width:166px;height:166px;flex-shrink:0}.widget-gallery .w-medium{width:352px;max-width:100%;height:166px}.widget-gallery .w-large{width:352px;max-width:100%;height:354px}.widget-gallery .w-brand{display:flex;align-items:center;gap:5px;white-space:nowrap;font-size:10px;font-weight:700;line-height:13px;color:var(--w-accent);min-width:0}.widget-gallery .w-brand svg{width:13px;height:13px;fill:currentColor;stroke:none;flex-shrink:0}.widget-gallery .w-widget-head{display:flex;justify-content:space-between;align-items:center;gap:5px;margin-bottom:10px}.widget-gallery .w-widget-date{font-size:10px;color:var(--w-sub);white-space:nowrap}.widget-gallery .w-urgent-label{font-size:12px;font-weight:650;color:var(--w-accent);margin-top:13px}.widget-gallery .w-count{font-size:36px;line-height:1.03;font-weight:740;letter-spacing:-1.5px;font-variant-numeric:tabular-nums;margin-top:2px}.widget-gallery .w-count small{font-size:17px;letter-spacing:-.5px;margin-left:3px;font-weight:600}.widget-gallery .w-small .w-current-title{font-size:12px;line-height:1.2;margin-top:10px;font-weight:650;max-width:135px}.widget-gallery .w-tiny{font-size:10px;line-height:1.3;color:var(--w-sub);margin-top:4px}.widget-gallery .w-small .w-place{font-size:10px;color:var(--w-sub);margin-top:3px}.widget-gallery .w-small .w-count-now{font-size:30px;letter-spacing:-1px;margin-top:5px}.widget-gallery .w-summary{display:grid;grid-template-columns:minmax(0,1fr) minmax(0,1fr);gap:12px;flex:1;min-height:0}.widget-gallery .w-summary-col{min-width:0;display:flex;flex-direction:column;align-items:stretch}.widget-gallery .w-summary-col+.w-summary-col{border-left:1px solid var(--w-line);padding-left:12px}.widget-gallery .w-eyeline{font-size:10px;line-height:1.2;font-weight:700;color:var(--w-accent);display:flex;align-items:center;gap:5px;margin-bottom:5px}.widget-gallery .w-now-dot{width:5px;height:5px;border-radius:50%;background:var(--w-green);display:inline-block}.widget-gallery .w-summary-title{font-size:13px;line-height:1.18;font-weight:680;display:block}.widget-gallery .w-room{font-size:10px;color:var(--w-sub);margin-top:4px;line-height:1.25}.widget-gallery .w-foot-cue{font-size:11px;font-weight:650;margin-top:auto;line-height:1.25;color:var(--w-accent)}.widget-gallery .w-foot-cue span{font-size:10px;font-weight:450;color:var(--w-sub);display:block;margin-top:3px}.widget-gallery .w-tappable{padding:0;text-align:left;color:inherit;background:transparent;min-width:0}.widget-gallery .w-summary button{display:flex;flex-direction:column;align-items:start;flex:1}.widget-gallery .w-leave-banner{display:flex;gap:10px;align-items:center;padding:10px 11px;background:var(--w-soft);border-radius:12px;margin:0 0 11px;color:var(--w-accent)}.widget-gallery .w-leave-banner strong{font-size:16px;letter-spacing:-.35px;line-height:1.15;display:block}.widget-gallery .w-leave-banner span{font-size:10px;display:block;line-height:1.25;margin-top:3px}.widget-gallery .w-arrow{width:25px;height:25px;border-radius:50%;background:var(--w-surface);display:grid;place-items:center;font-size:17px;flex-shrink:0}.widget-gallery .w-leave-banner .w-banner-text{min-width:0;flex:1}.widget-gallery .w-agenda{display:flex;flex-direction:column;gap:0;flex:1;min-height:0}.widget-gallery .w-agenda-row{display:grid;grid-template-columns:52px minmax(0,1fr);gap:8px;padding:9px 0;text-align:left;border-bottom:1px solid var(--w-line);flex:1;align-items:center;color:inherit;background:none;width:100%;min-height:0}.widget-gallery .w-agenda-row:last-child{border:0}.widget-gallery .w-row-time{font-size:11px;font-weight:650;font-variant-numeric:tabular-nums;align-self:center;color:var(--w-sub)}.widget-gallery .w-row-time span{display:block;font-size:9px;font-weight:500;margin-top:3px}.widget-gallery .w-row-title{font-size:13px;line-height:1.2;display:block;font-weight:650}.widget-gallery .w-row-meta{font-size:10px;color:var(--w-sub);line-height:1.3;margin-top:4px;display:block}.widget-gallery .w-current .w-row-time{color:var(--w-green)}.widget-gallery .w-current .w-row-title{color:var(--w-text)}.widget-gallery .w-free{border-top:1px solid var(--w-line);padding-top:9px;font-size:10px;display:flex;justify-content:space-between;gap:5px;color:var(--w-sub)}.widget-gallery .w-free strong{font-weight:600;color:var(--w-accent)}.widget-gallery .w-bottom-mark{height:3px;width:22px;border-radius:3px;background:var(--w-accent);opacity:.22;margin:6px 0 2px}.widget-gallery .w-empty{justify-content:flex-start}.widget-gallery .w-empty-heading{font-size:19px;letter-spacing:-.5px;font-weight:700;line-height:1.1;max-width:270px}.widget-gallery .w-empty .w-empty-heading{margin-top:14px}.widget-gallery .w-empty p{font-size:11px;color:var(--w-sub);line-height:1.4;margin-top:8px;max-width:240px}.widget-gallery .w-empty-action{font-size:11px;font-weight:650;color:var(--w-accent);margin-top:auto;min-height:30px;display:flex;align-items:center;padding:0}.widget-gallery .w-empty .w-suggest{display:flex;align-items:center;gap:8px;border-top:1px solid var(--w-line);padding:12px 0;width:100%;text-align:left;color:var(--w-text);background:none}.widget-gallery .w-suggest-time{font-size:11px;color:var(--w-accent);font-weight:650;width:46px;flex-shrink:0}.widget-gallery .w-suggest span:last-child{font-size:12px;font-weight:650;line-height:1.3}.widget-gallery .w-suggest small{font-size:10px;color:var(--w-sub);display:block;font-weight:450;margin-top:4px}.widget-gallery .w-empty .w-widget-head{margin-bottom:0}.widget-gallery .w-empty .w-suggestions{margin-top:15px}.widget-gallery .w-empty.w-medium{display:grid;grid-template-columns:1fr 1fr;gap:12px}.widget-gallery .w-empty.w-medium .w-empty-heading{font-size:19px;margin-top:12px}.widget-gallery .w-empty.w-medium .w-empty-side{border-left:1px solid var(--w-line);padding-left:12px;display:flex;flex-direction:column;justify-content:center}.widget-gallery .w-empty.w-medium .w-empty-side strong{font-size:12px;line-height:1.25}.widget-gallery .w-empty.w-medium .w-empty-side span{font-size:10px;color:var(--w-sub);margin-top:5px}.widget-gallery .w-empty.w-large .w-empty-heading{font-size:24px;margin-top:18px}.widget-gallery .w-empty.w-small .w-empty-heading{font-size:19px}.widget-gallery .w-empty.w-small p{font-size:10px}
.widget-gallery .w-native-note{display:grid;grid-template-columns:1fr 1fr;gap:24px;border-top:1px solid #dce2ea;margin-top:28px;padding-top:20px}.widget-gallery .w-native-note h3{font-size:13px;color:#324a65;margin-bottom:7px}.widget-gallery .w-native-note p{font-size:12px;line-height:1.6;color:#68778b}.widget-gallery .w-sources{display:flex;gap:14px;flex-wrap:wrap;margin-top:13px;font-size:11px}.widget-gallery .w-sources a{color:#00759f;text-decoration:underline;text-underline-offset:3px}.widget-gallery .w-accessibility-note{font-size:11px;line-height:1.55;color:#718197;margin-top:15px}.widget-gallery .w-material{--w-surface:#e4ebf5;--w-text:#1c2b3d;--w-sub:#526277;--w-line:#c9d4e3;--w-accent:#23587f;--w-soft:#ccdded;--w-green:#326845;border-radius:27px;box-shadow:none;padding:16px}.widget-gallery .w-material.w-small{width:166px;height:176px}.widget-gallery .w-material.w-medium{height:176px}.widget-gallery .w-material.w-large{height:368px}.widget-gallery .w-material .w-count{font-size:38px;font-weight:550}.widget-gallery .w-material .w-summary-title,.widget-gallery .w-material .w-row-title{font-weight:600}.widget-gallery .w-material .w-leave-banner{border-radius:18px;padding:12px}.widget-gallery .w-material .w-leave-banner strong{font-size:17px;font-weight:600}.widget-gallery .w-material .w-widget-head{margin-bottom:13px}.widget-gallery .w-material .w-small .w-urgent-label{margin-top:10px}.widget-gallery .w-material .w-free{padding-top:10px}.widget-gallery .w-material .w-empty-action{border-radius:20px;background:var(--w-soft);padding:0 12px;margin-top:10px;justify-content:center;min-height:34px}
.widget-gallery[data-w-appearance=dark]{--w-surface:#202329;--w-text:#f3f5f8;--w-sub:#b1b8c4;--w-line:#3b404b;--w-accent:#67d0f7;--w-soft:#143b4d;--w-green:#8cdda5;--w-wall:#202d40}.widget-gallery[data-w-appearance=dark] .w-material{--w-surface:#27384a;--w-text:#e8f0fa;--w-sub:#b6c6d9;--w-line:#4b5d72;--w-accent:#a5d4ff;--w-soft:#344f6d;--w-green:#9adcac}.widget-gallery[data-w-appearance=tinted]{--w-surface:#d7e7f0;--w-text:#102f46;--w-sub:#3d5d73;--w-line:#b1cbdc;--w-accent:#174e77;--w-soft:#c0d9e9;--w-green:#174e77;--w-wall:#c2d8e7}.widget-gallery[data-w-appearance=tinted] .w-material{--w-surface:#d1e4dc;--w-text:#163c33;--w-sub:#41675a;--w-line:#accbbb;--w-accent:#235e4f;--w-soft:#b6d5c4;--w-green:#235e4f}
.widget-gallery[data-w-large=true] .w-row-title{font-size:15px}.widget-gallery[data-w-large=true] .w-row-meta{font-size:11px}.widget-gallery[data-w-large=true] .w-summary-title{font-size:15px}.widget-gallery[data-w-large=true] .w-room{font-size:11px}.widget-gallery[data-w-large=true] .w-row-time{font-size:12px}.widget-gallery[data-w-large=true] .w-foot-cue{font-size:12px}.widget-gallery[data-w-large=true] .w-secondary-detail{display:none}.widget-gallery[data-w-large=true] .w-small .w-current-title{font-size:14px}.widget-gallery[data-w-large=true] .w-small .w-place{font-size:11px}.widget-gallery[data-w-large=true] .w-urgent-label{font-size:13px}.widget-gallery[data-w-large=true] .w-widget-date{font-size:11px}.widget-gallery[data-w-large=true] .w-agenda-row{padding:7px 0}.widget-gallery[data-w-large=true] .w-small .w-count{font-size:32px}.widget-gallery[data-w-large=true] .w-small .w-current-title{margin-top:7px}.widget-gallery[data-w-large=true] .w-summary-col+.w-summary-col{padding-left:9px}.widget-gallery[data-w-large=true] .w-summary{gap:9px}
.widget-gallery .w-dialog{width:min(390px,calc(100vw - 32px));max-height:calc(100vh - 60px);overflow:auto;border:0;border-radius:25px;background:#f2f2f7;color:#152235;padding:0;box-shadow:0 30px 80px #00193250}.widget-gallery .w-dialog::backdrop{background:#09153366;backdrop-filter:blur(3px)}.widget-gallery .w-dialog-head{display:flex;justify-content:space-between;align-items:center;padding:14px 18px;background:#fff;border-bottom:1px solid #e1e5eb;font-size:12px;font-weight:650}.widget-gallery .w-dialog-head button{color:#007ba8;min-height:38px;font-weight:600;background:none;padding:4px}.widget-gallery .w-dialog-body{padding:24px 22px}.widget-gallery .w-dialog h3{font-size:27px;line-height:1.1;letter-spacing:-.9px;margin:0 0 8px}.widget-gallery .w-dialog .w-dialog-room{font-size:13px;color:#67798e;margin-bottom:22px}.widget-gallery .w-dialog dl{background:#fff;border-radius:14px;padding:0 14px;margin:0 0 18px}.widget-gallery .w-dialog dl div{display:flex;justify-content:space-between;gap:12px;padding:15px 0;border-bottom:1px solid #e4e8ed;font-size:12px}.widget-gallery .w-dialog dl div:last-child{border:0}.widget-gallery .w-dialog dt{color:#61738a}.widget-gallery .w-dialog dd{margin:0;font-weight:650}.widget-gallery .w-dialog .w-dialog-note{font-size:13px;line-height:1.55;color:#567087}.widget-gallery .w-dialog .w-dialog-demo{font-size:10px;line-height:1.6;color:#79889c;margin-top:22px}.widget-gallery .w-dialog .w-dialog-button{width:100%;border-radius:13px;background:#007ba8;color:#fff;padding:14px;margin-top:21px;min-height:46px;font-size:14px;font-weight:650}
.widget-gallery .w-status.w-small .w-empty-heading{margin-top:10px;font-size:18px;line-height:1.12}.widget-gallery .w-status.w-small p{margin-top:6px;font-size:10px;line-height:1.3}.widget-gallery .w-status.w-medium .w-empty-side p{font-size:11px}.widget-gallery .w-status-lines{margin-top:17px}.widget-gallery .w-status-line{padding:13px 0;border-top:1px solid var(--w-line);font-size:13px;font-weight:650}.widget-gallery .w-status-line span{display:block;font-size:11px;color:var(--w-sub);font-weight:400;line-height:1.4;margin-top:5px}.widget-gallery .w-history-label{font-size:10px;letter-spacing:.7px;font-weight:650;color:var(--w-sub);margin-top:14px}.widget-gallery .w-status.w-large .w-empty-heading{font-size:24px;line-height:1.12}.widget-gallery .w-status.w-large .w-agenda{margin-top:4px}.widget-gallery .w-status.w-large .w-agenda-row{padding:7px 0}.widget-gallery .w-status.w-large .w-row-title{font-size:12px}.widget-gallery .w-status.w-large .w-empty-action{margin-top:8px;flex-shrink:0}.widget-gallery .w-upcoming-name{font-size:14px;font-weight:700;line-height:1.18;display:block;margin-top:9px}.widget-gallery .w-upcoming-count{font-size:36px;font-weight:730;letter-spacing:-1.2px;line-height:1.1;color:var(--w-accent);font-variant-numeric:tabular-nums;margin-top:8px;display:block}.widget-gallery .w-upcoming-count small{font-size:16px;font-weight:550;letter-spacing:-.3px;margin-left:4px}.widget-gallery .w-upcoming-date{font-size:11px;color:var(--w-sub);margin-top:5px;display:block}.widget-gallery .w-upcoming.w-small .w-empty-action{min-height:23px;font-size:10px;margin-top:auto}.widget-gallery .w-upcoming.w-medium .w-upcoming-name{font-size:15px;margin-top:0}.widget-gallery .w-upcoming.w-medium .w-upcoming-count{margin-top:6px}.widget-gallery .w-upcoming.w-medium .w-summary-col+.w-summary-col{justify-content:center}.widget-gallery .w-upcoming.w-medium .w-empty-action{font-size:11px;min-height:26px}.widget-gallery .w-upcoming.w-medium .w-upcoming-date{font-size:15px;font-weight:650;color:var(--w-text);margin-top:0}.widget-gallery .w-upcoming.w-medium .w-tiny{font-size:11px;line-height:1.5;margin-top:5px}.widget-gallery .w-upcoming-top{display:flex;align-items:end;justify-content:space-between;gap:15px;margin-bottom:13px}.widget-gallery .w-upcoming.w-large .w-upcoming-count{margin-top:0;font-size:39px}.widget-gallery .w-upcoming.w-large .w-upcoming-date{font-size:12px;margin-top:0;text-align:right;line-height:1.4}.widget-gallery .w-upcoming.w-large .w-history-label{margin-top:0}.widget-gallery .w-upcoming.w-large .w-empty-action{margin-top:5px;flex-shrink:0}.widget-gallery .w-upcoming.w-large .w-agenda-row{padding:8px 0}.widget-gallery .w-upcoming.w-large .w-row-title{font-size:12px}.widget-gallery .w-upcoming.w-large .w-row-meta{font-size:10px}.widget-gallery .w-state-rule{border-left:3px solid #b7cfe1;padding:4px 0 4px 12px;font-size:11px;line-height:1.55;color:#60758a;margin:0 0 19px}.widget-gallery[data-w-large=true] .w-status.w-large .w-row-title,.widget-gallery[data-w-large=true] .w-upcoming.w-large .w-row-title{font-size:14px}.widget-gallery[data-w-large=true] .w-upcoming.w-small .w-upcoming-name{font-size:15px}.widget-gallery[data-w-large=true] .w-upcoming.w-small .w-upcoming-count{font-size:33px}.widget-gallery[data-w-large=true] .w-small .w-urgent-label{margin-top:9px}
.widget-gallery .w-upcoming.w-medium .w-empty-action{flex-direction:row;align-items:center;white-space:nowrap}
@media(max-width:1050px){.widget-gallery{padding:12px 24px 35px}.widget-gallery .w-grid{grid-template-columns:190px minmax(280px,1fr);gap:24px}.widget-gallery .w-specimen-large{grid-column:1/-1;display:grid;grid-template-columns:190px minmax(280px,1fr);gap:0 24px}.widget-gallery .w-specimen-large .w-caption{grid-column:1/-1}.widget-gallery .w-specimen-large .w-canvas{grid-column:2;grid-row:2/5}.widget-gallery .w-specimen-large .w-explain{grid-column:1;grid-row:2;margin-top:0}.widget-gallery .w-specimen-large .w-rendering{grid-column:1}.widget-gallery .w-check{margin-left:0}.widget-gallery .w-intro{align-items:start}.widget-gallery .w-intro p{max-width:320px}}
@media(max-width:650px){.widget-gallery{padding:10px 18px 30px}.widget-gallery .w-intro{display:block}.widget-gallery h2{font-size:30px}.widget-gallery .w-intro p{margin-top:14px;max-width:none}.widget-gallery .w-controls{gap:10px}.widget-gallery .w-segment button{padding:7px 11px}.widget-gallery .w-grid{display:flex;flex-direction:column;gap:24px}.widget-gallery .w-specimen{width:100%}.widget-gallery .w-specimen-large{display:block}.widget-gallery .w-canvas{min-height:210px}.widget-gallery .w-specimen-large .w-canvas{min-height:400px}.widget-gallery .w-specimen-large .w-explain{margin-top:13px}.widget-gallery .w-native-note{grid-template-columns:1fr;gap:20px}.widget-gallery .w-sample-line{display:block}.widget-gallery .w-sample-line span{display:block;margin-top:4px}.widget-gallery .w-medium,.widget-gallery .w-large{width:352px}.widget-gallery .w-design-note{margin-top:12px;padding-top:12px}.widget-gallery .w-material.w-medium{min-height:176px}.widget-gallery .w-widget-date{font-size:9px}.widget-gallery .w-brand{font-size:9px}}
@media(prefers-reduced-motion:reduce){.widget-gallery *{scroll-behavior:auto!important;transition:none!important}}
`;

export function mountWidgets(root: HTMLElement): void {
  let platform: WidgetPlatform = "ios";
  let state: WidgetState = "now";
  let appearance: WidgetAppearance = "light";
  let largeText = false;
  root.innerHTML = `<style>${widgetStyles}</style><section class="widget-gallery" aria-label="ConPaws widget concepts"></section>`;
  const gallery = root.querySelector<HTMLElement>(".widget-gallery")!;

  const brand = (full = false): string => `<span class="w-brand">${widgetMark}${full ? "Lakeside Fur Con" : "ConPaws"}</span>`;
  const header = (): string => `<div class="w-widget-head">${brand(true)}<span class="w-widget-date">SAT 19</span></div>`;
  const surface = (): string => platform === "android" ? "w-surface w-material" : "w-surface";
  const departure = (): string => state === "leave" ? "Leave now" : "Leave at 2:25";

  function row(event: WidgetEvent, time: string, meta: string, current = false): string {
    return `<button class="w-agenda-row w-tappable${current ? " w-current" : ""}" data-w-event="${event}" aria-label="${widgetEvents[event].title}, ${time}, ${meta}"><span class="w-row-time">${time}${current ? "<span>HERE NOW</span>" : ""}</span><span><strong class="w-row-title">${widgetEvents[event].title}</strong><span class="w-row-meta">${meta}</span></span></button>`;
  }

  function upcomingWidget(size: "small" | "medium" | "large"): string {
    const action = `<button class="w-empty-action w-tappable" data-w-context="true">Open ConPaws <span aria-hidden="true">&nbsp;›</span></button>`;
    const count = `<strong class="w-upcoming-count">15<small>days</small></strong>`;
    if (size === "small") return `<button class="${surface()} w-small w-upcoming" data-w-context="true" aria-label="Lakeside Fur Con, in 15 days, September 18 to 20. Open ConPaws.">${brand()}<strong class="w-upcoming-name">Lakeside Fur Con</strong>${count}<span class="w-upcoming-date">September 18–20</span><span class="w-empty-action">Open ConPaws <span aria-hidden="true">&nbsp;›</span></span></button>`;
    if (size === "medium") return `<div class="${surface()} w-medium w-upcoming"><div class="w-widget-head">${brand()}<span class="w-widget-date">COMING UP</span></div><div class="w-summary"><div class="w-summary-col"><strong class="w-upcoming-name">Lakeside Fur Con</strong>${count}</div><div class="w-summary-col"><strong class="w-upcoming-date">September 18–20</strong><span class="w-tiny">3 panels picked<br><span class="w-secondary-detail">Your plan is saved.</span></span>${action}</div></div></div>`;
    return `<div class="${surface()} w-large w-upcoming"><div class="w-widget-head">${brand(true)}<span class="w-widget-date">COMING UP</span></div><div class="w-upcoming-top">${count}<span class="w-upcoming-date">September 18–20<br>3 panels picked</span></div><span class="w-history-label">YOUR SATURDAY PLAN · SEPTEMBER 19</span><div class="w-agenda">${row("photo", "2:00", "Ballroom A · Leave 2:25")}${row("design", "2:35", "Cedar · Your stop until 3:20")}${row("story", "4:00", "Full session · Until 5:00")}</div>${action}</div>`;
  }

  function statusWidget(size: "small" | "medium" | "large"): string {
    if (state === "upcoming") return upcomingWidget(size);
    if (state === "now" || state === "leave") return "";
    const copy = widgetStatusCopy[state];
    const action = `<button class="w-empty-action w-tappable" data-w-context="true">${copy.action} <span aria-hidden="true">&nbsp;›</span></button>`;
    if (size === "small") return `<button class="${surface()} w-small w-empty w-status" data-w-context="true" aria-label="${copy.title}. ${copy.body}">${brand()}<strong class="w-empty-heading">${copy.title}</strong><p>${copy.body}</p><span class="w-empty-action">${copy.action} <span aria-hidden="true">&nbsp;›</span></span></button>`;
    if (size === "medium") return `<div class="${surface()} w-medium w-empty w-status"><div>${brand()}<strong class="w-empty-heading">${copy.title}</strong><p>${state === "noConvention" ? "Check your conventions." : "Lakeside Fur Con"}</p></div><div class="w-empty-side"><strong>${state === "finished" ? "3 panels in your plan" : state === "empty" ? "Make room for your favorites." : "Choose your convention."}</strong><p>${copy.body}</p>${action}</div></div>`;
    const content = state === "finished"
      ? `<span class="w-history-label">YOUR EARLIER PICKS · SATURDAY</span><div class="w-agenda">${row("photo", "2:00", "Planned until 2:25")}${row("design", "2:35", "Planned until 3:20")}${row("story", "4:00", "Planned until 5:00")}</div>`
      : state === "empty"
        ? `<div class="w-status-lines"><div class="w-status-line">Choose your panels<span>Open the schedule in ConPaws and add panels to your plan.</span></div><div class="w-status-line">Keep the next step close<span>Your next saved panel and leave reminders appear here.</span></div></div>`
        : `<div class="w-status-lines"><div class="w-status-line">Before the convention<span>See your convention dates and a countdown.</span></div><div class="w-status-line">During the convention<span>See your picked panels and leave reminders.</span></div></div>`;
    return `<div class="${surface()} w-large w-empty w-status">${state === "noConvention" ? `<div class="w-widget-head">${brand()}<span class="w-widget-date">OPEN APP</span></div>` : header()}<strong class="w-empty-heading">${copy.title}</strong><p>${state === "finished" ? "Your saved panels have ended. Review your plan in ConPaws." : copy.detail}</p>${content}${action}</div>`;
  }

  function small(): string {
    if (state !== "now" && state !== "leave") return statusWidget("small");
    return `<button class="${surface()} w-small" data-w-event="${state === "leave" ? "design" : "photo"}" aria-label="${state === "leave" ? "Leave now" : "Leave in 7 minutes at 2:25 PM"}. Join Character design lab at 2:35 PM, Cedar.">${brand()}<span class="w-urgent-label">${state === "leave" ? "Time to go" : "Leave in"}</span><span class="w-count${state === "leave" ? " w-count-now" : ""}">${state === "leave" ? "Now" : "7<small>min</small>"}</span><strong class="w-current-title">${state === "leave" ? "Character design lab" : "Fursuit photography"}</strong><span class="w-place">${state === "leave" ? "Join 2:35 · Cedar" : "At 2:25 · Ballroom A"}</span></button>`;
  }

  function medium(): string {
    if (state !== "now" && state !== "leave") return statusWidget("medium");
    return `<div class="${surface()} w-medium">${header()}<div class="w-summary"><div class="w-summary-col"><button class="w-tappable" data-w-event="photo"><span class="w-eyeline"><i class="w-now-dot"></i>${state === "leave" ? "LEAVING" : "HERE NOW"}</span><strong class="w-summary-title">Fursuit photography</strong><span class="w-room">Ballroom A</span><span class="w-foot-cue">${departure()}<span class="w-secondary-detail">Your stop ends early</span></span></button></div><div class="w-summary-col"><button class="w-tappable" data-w-event="design"><span class="w-eyeline">JOIN 2:35 PM</span><strong class="w-summary-title">Character design lab</strong><span class="w-room">Cedar</span><span class="w-foot-cue">10 min buffer<span class="w-secondary-detail">Stay until 3:20</span></span></button></div></div></div>`;
  }

  function large(): string {
    if (state !== "now" && state !== "leave") return statusWidget("large");
    return `<div class="${surface()} w-large">${header()}<button class="w-leave-banner w-tappable" data-w-event="design"><i class="w-arrow" aria-hidden="true">↗</i><span class="w-banner-text"><strong>${state === "leave" ? "Leave now for Cedar" : "Leave in 7 min · 2:25"}</strong><span>Join Character design lab at 2:35</span></span></button><div class="w-agenda">${row("photo", "2:00", "Ballroom A · Leave 2:25", true)}${row("design", "2:35", "Cedar · Your stop until 3:20")}${row("story", "4:00", "Full session · Until 5:00")}</div><div class="w-free"><span>3:20–4:00</span><strong>40 min free between stops</strong></div></div>`;
  }

  function render(): void {
    gallery.dataset.wAppearance = appearance;
    gallery.dataset.wLarge = String(largeText);
    const android = platform === "android";
    const active = state === "now" || state === "leave";
    const sampleLabel = state === "noConvention" ? "No convention saved · Sample state" : state === "upcoming" ? "Lakeside Fur Con · Demo date: Thu, Sep 3" : `Lakeside Fur Con · Sat, Sep 19 · ${state === "leave" ? "2:25" : state === "finished" ? "5:00" : "2:18"} PM`;
    const stateNote = state === "upcoming" ? "Sample convention dates: September 18–20. The countdown counts calendar days to September 18, not an opening time. Three panels are already picked." : state === "noConvention" ? "Sample condition: no convention saved. Missing widget data currently uses the same native fallback, so this mockup does not diagnose a sync failure." : state === "empty" ? "Sample condition: a convention is saved with no active panels in the widget snapshot. Cancelled, removed, or invalid picked panels are filtered out too. The snapshot does not reveal whether the full schedule has been published." : state === "finished" ? "Sample condition: every saved panel has ended, with no panel running and none still to come. The convention itself is still in progress." : "";
    const smallExplain = active ? "Leave time stays prominent. Your room stays one line away." : state === "upcoming" ? "Convention name, dates, and countdown stay together. Tap to open ConPaws." : "Name the state clearly and offer one useful next action.";
    const mediumExplain = active ? "The leave time and join time sit together. Both overlapping sessions stay in your plan." : state === "upcoming" ? "A wider layout adds the number of panels already picked and a direct path back to your plan." : "Keep the saved convention visible when one exists. The action opens the matching app destination.";
    const largeExplain = active ? "Three stops, a leave cue, and a 40-minute opening. Extra height adds useful context." : state === "upcoming" ? "Extra height adds your saved Saturday picks. The countdown leads without taking over the whole widget." : state === "finished" ? "Show the earlier plan for context. A picked panel is not proof that someone attended it." : "Use the space to explain how the widget becomes useful. Do not invent panel suggestions from data the widget does not have.";
    gallery.innerHTML = `
      <div class="w-intro"><div><div class="w-kicker">The next step, at a glance</div><h2>More useful.<br><span>Every size.</span></h2></div><p>See when to leave, where to join, and what comes after. Extra space earns its place with your actual day.</p></div>
      <div class="w-controls" aria-label="Widget preview controls">
        <div class="w-segment" role="group" aria-label="Widget platform"><button data-w-platform="ios" aria-pressed="${!android}">iOS widgets</button><button data-w-platform="android" aria-pressed="${android}">Android widgets</button></div>
        <label class="w-control-label">Moment <select data-w-state="true" aria-label="Widget scenario">${Object.entries(widgetScenarioLabels).map(([value, label]) => `<option value="${value}"${state === value ? " selected" : ""}>${label}</option>`).join("")}</select></label>
        <label class="w-control-label">Look <select data-w-appearance="true" aria-label="Widget appearance"><option value="light"${appearance === "light" ? " selected" : ""}>Light</option><option value="dark"${appearance === "dark" ? " selected" : ""}>Dark</option><option value="tinted"${appearance === "tinted" ? " selected" : ""}>${android ? "Wallpaper color" : "Tinted"}</option></select></label>
        <label class="w-control-label w-check"><input type="checkbox" data-w-large="true"${largeText ? " checked" : ""}>Larger text</label>
      </div>
      <div class="w-sample-line"><strong>${sampleLabel}</strong><span>Fictional sample data · Tap a widget or event to preview its destination</span></div>
      ${stateNote ? `<p class="w-state-rule">${stateNote}</p>` : ""}
      <div class="w-grid">
        <section class="w-specimen"><h3 class="w-caption">${android ? "Compact" : "Small"}<small>${android ? "2 × 2 example" : "One action"}</small></h3><div class="w-canvas">${small()}</div><p class="w-explain"><strong>${active ? "The next decision." : "One clear starting point."}</strong>${smallExplain}</p><div class="w-design-note"><span class="w-note-number">01</span><p><strong>Keep the type readable.</strong><br>Reduce detail before shrinking text.</p></div></section>
        <section class="w-specimen"><h3 class="w-caption">${android ? "Wide" : "Medium"}<small>${android ? "4 × 2 example" : active ? "Now + next" : "Your convention"}</small></h3><div class="w-canvas">${medium()}</div><p class="w-explain"><strong>${active ? "Two panels. One transition." : "A little more context."}</strong>${mediumExplain}</p><div class="w-design-note"><span class="w-note-number">02</span><p><strong>Your times, clearly labeled.</strong><br>Widgets show the part you plan to attend. Event details keep the official session times.</p></div><p class="w-rendering">${android ? "Size labels illustrate one launcher grid. Actual dimensions depend on the launcher and available space." : "Illustrated WidgetKit families. Final layout uses system margins and semantic text styles."}</p></section>
        <section class="w-specimen w-specimen-large"><h3 class="w-caption">${android ? "Tall" : "Large"}<small>${android ? "4 × 4 example" : active ? "Rest of your day" : state === "finished" ? "Earlier picks" : "Plan ahead"}</small></h3><div class="w-canvas w-canvas-large">${large()}</div><p class="w-explain"><strong>Extra space earns its place.</strong>${largeExplain}</p></section>
      </div>
      <div class="w-native-note"><div><h3>${android ? "Built for the Android home screen" : "Built for the iOS home screen"}</h3><p>${android ? "Use native App Widgets with Jetpack Glance and responsive layouts. Compact, wide, and tall layouts adapt their content to available space. Wallpaper colors use Android's dynamic color system." : "Use WidgetKit and SwiftUI with system text, margins, rendering modes, and deep links. Keep the leave cue readable when iOS applies a tint. NativeWind stays in the React Native app."}</p><div class="w-sources">${android ? '<a href="https://developer.android.com/develop/ui/compose/glance/build-ui" target="_blank" rel="noreferrer">Glance responsive layouts</a><a href="https://developer.android.com/design/ui/mobile/guides/widgets/style" target="_blank" rel="noreferrer">Android widget style</a>' : '<a href="https://developer.apple.com/design/human-interface-guidelines/widgets" target="_blank" rel="noreferrer">Apple widget guidance</a><a href="https://developer.apple.com/documentation/widgetkit/creating-a-widget-extension" target="_blank" rel="noreferrer">WidgetKit</a>'}</div></div><div><h3>Preview boundaries</h3><p>These are browser mockups, not installed widgets. Moment controls change sample state; they do not schedule reminders. The 10-minute buffer is part of your plan, not a measured walking route. “Larger text” checks density; real font scaling and refresh timing need device testing.</p><p class="w-accessibility-note">${largeText ? "Larger text removes secondary lines while keeping leave, join, and room information." : "Switch on larger text to test fewer details without losing your next action."} Appearance controls approximate system rendering.</p></div></div>
      <p class="w-accessibility-note">Widget scenarios are independent samples; edits in Phone flow do not update them.</p>
      <dialog class="w-dialog" data-w-dialog="true" aria-labelledby="w-dialog-title"><div class="w-dialog-head"><span>Widget destination preview</span><button data-w-close="true">Done</button></div><div class="w-dialog-body" data-w-dialog-body="true"></div></dialog>`;
  }

  function openEvent(event: WidgetEvent): void {
    const detail = widgetEvents[event];
    gallery.querySelector<HTMLElement>("[data-w-dialog-body]")!.innerHTML = `<h3 id="w-dialog-title">${detail.title}</h3><p class="w-dialog-room">${detail.room}</p><dl><div><dt>Official session</dt><dd>${detail.official}</dd></div><div><dt>${state === "finished" ? "You planned" : "You're attending"}</dt><dd>${detail.attending}</dd></div></dl><p class="w-dialog-note">${state === "finished" ? "These are the times you planned to attend." : detail.note}</p><button class="w-dialog-button" data-w-close="true">Back to widgets</button><p class="w-dialog-demo">Fictional event · Proposed destination: the matching event and your planned attendance details. Current native widgets open the convention schedule.</p>`;
    gallery.querySelector<HTMLDialogElement>("[data-w-dialog]")!.showModal();
  }

  gallery.addEventListener("click", (event) => {
    const target = (event.target as HTMLElement).closest<HTMLButtonElement>("button");
    if (!target) return;
    if (target.dataset.wPlatform === "ios" || target.dataset.wPlatform === "android") {
      platform = target.dataset.wPlatform;
      render();
      gallery.querySelector<HTMLButtonElement>(`[data-w-platform="${platform}"]`)?.focus({ preventScroll: true });
    } else if (target.dataset.wEvent && target.dataset.wEvent in widgetEvents) {
      openEvent(target.dataset.wEvent as WidgetEvent);
    } else if (target.dataset.wClose) {
      gallery.querySelector<HTMLDialogElement>("[data-w-dialog]")?.close();
    } else if (target.dataset.wContext) {
      const title = state === "upcoming" ? "Your convention is coming up." : state === "noConvention" ? "Choose your convention." : state === "finished" ? "Review your plan." : "Choose your panels.";
      const body = state === "upcoming" ? "Lakeside Fur Con is saved for September 18–20. Open its schedule in ConPaws to review your three picked panels." : state === "noConvention" ? "ConPaws opens to your conventions. Choose a convention or add one with your own dates." : state === "finished" ? "All panels in this sample plan have ended. Open your convention schedule in ConPaws to review your earlier picks." : "ConPaws opens your convention schedule. Add panels to your plan to see your next panel and leave reminders here.";
      const action = state === "finished" ? '<button class="w-dialog-button" data-w-close="true">Back to widgets</button>' : `<button class="w-dialog-button" data-w-fill="${state === "noConvention" ? "upcoming" : "now"}">${state === "noConvention" ? "Load a sample convention" : state === "upcoming" ? "Preview Saturday's plan" : "Load the sample plan"}</button>`;
      gallery.querySelector<HTMLElement>("[data-w-dialog-body]")!.innerHTML = `<h3 id="w-dialog-title">${title}</h3><p class="w-dialog-room">${state === "noConvention" ? "ConPaws · App home" : "Lakeside Fur Con · September 18–20"}</p><p class="w-dialog-note">${body}</p>${action}<p class="w-dialog-demo">Preview action only. Native widget taps open ConPaws; the buttons here demonstrate fictional states.</p>`;
      gallery.querySelector<HTMLDialogElement>("[data-w-dialog]")!.showModal();
    } else if (target.dataset.wFill === "now" || target.dataset.wFill === "upcoming") {
      state = target.dataset.wFill;
      render();
      gallery.querySelector<HTMLSelectElement>("select[data-w-state]")?.focus({ preventScroll: true });
    }
  });

  gallery.addEventListener("change", (event) => {
    const target = event.target as HTMLSelectElement | HTMLInputElement;
    let restore = "";
    if (target.matches("select[data-w-state]") && Object.hasOwn(widgetScenarioLabels, target.value)) {
      state = target.value as WidgetState;
      restore = "select[data-w-state]";
    } else if (target.matches("select[data-w-appearance]") && ["light", "dark", "tinted"].includes(target.value)) {
      appearance = target.value as WidgetAppearance;
      restore = "select[data-w-appearance]";
    } else if (target.matches("input[data-w-large]")) {
      largeText = (target as HTMLInputElement).checked;
      restore = "input[data-w-large]";
    }
    if (restore) {
      render();
      gallery.querySelector<HTMLElement>(restore)?.focus({ preventScroll: true });
    }
  });
  render();
}
