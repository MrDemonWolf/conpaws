// NativeWind v5 is built on react-native-css, which ships the `className`
// prop augmentation for React Native components.
//
// Do NOT use `/// <reference types="nativewind/types" />` — nativewind
// 5.0.0-preview.2 maps "./types" to a `types.d.ts` that is not actually
// published, and its export condition is "typescript" rather than "types",
// so TypeScript cannot resolve it either way.
/// <reference types="react-native-css/types" />
