/**
 * The exact wording shown next to the signup button.
 *
 * It is stored verbatim on every waitlist row. If this string changes, older
 * rows keep the text those people actually agreed to — which is the entire
 * point of recording it rather than a boolean.
 */
export const CONSENT_COPY =
  "By joining the waitlist you agree to receive occasional email updates about ConPaws. Confirm your address by clicking the link we send. Unsubscribe any time.";
